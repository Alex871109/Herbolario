{% extends 'base.html.twig' %}

{% block contenedor_principal %}
    {% include 'comunes/_mensajes.html.twig' %}

    <table class="table">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Usos</th>
      <th scope="col">Acciones</th>
    </tr>
  </thead>
  <tbody>
  {% for uso in  usos  %}
    <tr>
      <th scope="row">{{uso.id}}</th>
      <td>{{uso.nombre}}</td>
      <td>
        <a href="{{path('app_usos_editar',{id:uso.id})}}" class="btn btn-link mg-3">
            <span class="oi oi-pencil" title="Editar" aria-hidden="true"></span>
        </a>
      </td>
    </tr>
  {% endfor %}
  </tbody>
</table>

    <div class="d-md-flex my-2 my-lg-0">
        <a class="btn btn-outline-success mr-2 w-100 mb-3 mb-sm-0" title="Gestión de usos" href="{{path('app_nuevo_usos')}}">
            Nuevo
        </a>
    </div>

{% endblock %}



